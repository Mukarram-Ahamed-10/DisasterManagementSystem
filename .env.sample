# Database Configuration
DATABASE_URL=postgresql://username:password@localhost:5432/disaster_db
PGHOST=localhost
PGPORT=5432
PGUSER=disaster_user
PGPASSWORD=your_password_here
PGDATABASE=disaster_db

# Application Security
SESSION_SECRET=your-very-long-and-random-secret-key-here-change-this-in-production

# Instructions:
# 1. Copy this file to .env
# 2. Replace the placeholder values with your actual database credentials
# 3. Generate a strong SESSION_SECRET (at least 32 characters)
# 4. Never commit .env to version control